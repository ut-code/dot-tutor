type SixDotsType = [boolean, boolean, boolean, boolean, boolean, boolean];

type EightDotsType = [
  boolean,
  boolean,
  boolean,
  boolean,
  boolean,
  boolean,
  boolean,
  boolean,
];

export type DotsType = SixDotsType | EightDotsType;
